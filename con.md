graph TD     %% การเริ่มต้นระบบ     Start([เปิดหน้าเว็บ window.onload]) --> Init[initializeLiff]     Init -- ตรวจสอบสิทธิ์ LINE --> CheckLogin{liff.isLoggedIn?}      CheckLogin -- ไม่ได้ล็อกอิน --> ShowLoginBtn[แสดงปุ่มเข้าสู่ระบบ]     ShowLoginBtn -- คลิกปุ่ม --> loginWithLine[loginWithLine]     loginWithLine -- เรียก LINE SDK --> LiffLogin[liff.login]      CheckLogin -- ล็อกอินสำเร็จ --> SetupUI[ดึงข้อมูลโปรไฟล์ & เปิดหน้าหลัก]     SetupUI --> FetchData[fetchDataFromGAS]      %% กลุ่มการดึงข้อมูลและแสดงผล     subgraph 1. ระบบหน้าหลัก (Dashboard)         FetchData -- เรียก API (action: 'getPending') --> CallGas1((callGas))         CallGas1 -- ดึงข้อมูลสำเร็จ --> Render[renderDashboard]         CallGas1 -- มีข้อมูลรอซิงค์ --> Sync[triggerBackgroundSync]         CallGas1 -- ขัดข้อง/ล้มเหลว --> ErrMod1[showErrorModal]                  Render -- แสดงตารางข้อมูล --> Table[ตารางรายการ]         Table -- คลิกสลับแท็บ --> Switch[switchTab]         Switch --> Render     end      %% กลุ่มการจัดการสถานะในตาราง     subgraph 2. การจัดการสถานะ (ตารางรายการ)         Table -- คลิก 'เพิกถอนหมายจับ' --> ActRevoke[actionRevoke]         ActRevoke -- กดยืนยัน (action: 'markRevoked') --> CallGas2((callGas))         CallGas2 -- สำเร็จ --> Reload1[fetchDataFromGAS]          Table -- คลิก 'ส่งต่อสำนวน' --> ActFwd[actionForward]         ActFwd -- กดยืนยัน (action: 'markForwarded') --> CallGas3((callGas))         CallGas3 -- สำเร็จ --> Reload2[fetchDataFromGAS]     end      %% กลุ่มฟอร์มเพิ่มรายการ     subgraph 3. ระบบเพิ่มรายการใหม่ (Modal Form)         Table -- คลิก 'เพิ่มรายการใหม่ (+)' --> OpenForm[openFormModal]         OpenForm -- กรอกคำค้นหา + คลิก 'ค้นหา' --> Search[searchWarrants]                  Search -- เรียก API (action: 'search') --> CallGas4((callGas))         CallGas4 -- พบข้อมูล --> ShowForm[แสดงผลลัพธ์และฟอร์มให้กรอกต่อ]         ShowForm -- เลือกเงื่อนไข 'อื่นๆ' --> ToggleReason[toggleReasonDetail]                  ShowForm -- กรอกข้อมูลครบ + กด 'ตรวจสอบและบันทึก' --> Submit[handleFormSubmit]         Submit -- จัดเตรียมข้อมูล & ตรวจสอบ --> OpenConfirm[openConfirmModal]          OpenConfirm -- คลิก 'ย้อนกลับไปแก้ไข' --> Cancel[closeConfirmModal: false]         Cancel --> ShowForm          OpenConfirm -- คลิก 'ยืนยันบันทึกข้อมูล' --> ConfirmYes[closeConfirmModal: true]         ConfirmYes -- เรียก API (action: 'addProcess') --> CallGas5((callGas))                  CallGas5 -- สำเร็จ --> ShowToast[showToast แจ้งเตือนสำเร็จ]         ShowToast --> CloseForm[closeFormModal]         CloseForm --> Reload3[fetchDataFromGAS]     end      %% ตกแต่งสีให้ดูง่ายขึ้น     classDef api fill:#f97316,stroke:#c2410c,stroke-width:2px,color:#fff;     class CallGas1,CallGas2,CallGas3,CallGas4,CallGas5 api;
